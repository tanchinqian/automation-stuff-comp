/**
 * Active dataset selection. Flip this to swap the bundled inspection images
 * without touching the CV pipeline or UI.
 */
export const DATASET_CONFIG = {
  activeDataset: 'pcb-aoi',
} as const;
